export interface Specialty {
  id: string;
  name: string;
  icon: React.ReactNode;
  link: string;
}

export interface Step {
  id: number;
  title: string;
  description: string;
  icon: React.ReactNode;
}